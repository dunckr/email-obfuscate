export default () => {
  return 'Hello Lib Starter';
};

//function emailObfuscate(’.selector’, {
	//name: ‘test’,
	//domain: ‘example’,
	//tld: ‘com’
//});

// use case to prevent
// https://en.wikipedia.org/wiki/Email_address_harvesting
// difficulties match style
// allow interaction the same

// create element to get the size - hidden = pseudo
// display to none
// also get the attributes
// can override and just set options

// also check the style on hover

// create the canvas
// must have extra pixels - down size for retina
// positioning difficult  - ctx.textAlign="left";

// add text to the canvas that matches the pseduo
// provide overrides

// change the text on hover similar to default behaviour

// add click event to open email address
// provide shorthand to set subject etc
// make sure can just use default href will override

// example page just shows different text and sizes with colors
// left original right the obfuscated versions
